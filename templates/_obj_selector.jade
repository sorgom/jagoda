.objSel 
    {% for id, src, label, class, wlabel, wclass in items %}
    a(href="/{{action}}/{{id}}")
        div 
            div
                img(src="{{src}}")
        div {{id}}
        div(class="{{class}}") {{label}}         
        div(class="{{wclass}}") {{wlabel}}
    {% endfor %}